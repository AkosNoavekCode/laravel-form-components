<div>
    <input type="checkbox" wire:model="{{$name}}" name="{{ $name }}" class="btn-check" id="{{ $id }}" autocomplete="off">
    <label class="btn btn-outline-primary" for="{{ $id }}">{{ $label }}</label>
</div>
